# Single-env runtime packages for the movie dubbing pipeline.
# Expanded to cover CosyVoice + Matcha-TTS runtime imports on Windows.
# Keep torch / torchaudio as-is in the current environment to avoid breaking ASR.

qwen-asr>=0.0.6
audio-separator[gpu]>=0.28.0
silero-vad>=6.2.1
# DiariZen 은 git clone + pip install -e . 별도 설치 필요 — Docker 사용 권장.
# 단일 env 사용 시: git clone --recurse-submodules https://github.com/BUTSpeechFIT/DiariZen.git && cd DiariZen && pip install -r requirements.txt && pip install -e .

HyperPyYAML==1.2.3
modelscope==1.20.0
inflect==7.3.1
onnx==1.16.0
onnxruntime==1.18.0
omegaconf==2.3.0
lightning==2.2.4
librosa==0.10.2
soundfile==0.12.1
pyworld==0.3.4
wetext==0.0.4
x-transformers==2.11.24
conformer==0.3.2
transformers==4.51.3
diffusers==0.29.0
hydra-core==1.3.2
gdown==5.1.0
networkx==3.1
matplotlib==3.7.5
protobuf==4.25.0
pyarrow==18.1.0
pydantic==2.7.0
rich==13.7.1
wget==3.2
numpy==1.26.4
einops
scipy
Unidecode
rootutils
torchmetrics
torchvision
pandas
seaborn
Cython
phonemizer
piper_phonemize
